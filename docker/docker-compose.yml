version: "3.8"

services:
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    container_name: croatian-learning-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - croatian-network

networks:
  croatian-network:
    driver: bridge